const initialState = {
    words: [],
    antonyms: [],
    synonyms: [],
    detail: []
};

export const rootReducer = (state = initialState, { type, action }) => {
    switch(type) {
        default: 
            return state
    }
};